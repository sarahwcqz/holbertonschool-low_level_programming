#include "main.h"
#include <stdio.h>
/**
 * main - entry point, we want a prgm that prints its name
 * @argc: the number of args
 * @argv: the array containing the args
 * Return: 0
 */
int main(int argc, char* argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
